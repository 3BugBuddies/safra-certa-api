spring:
  application:
    name: safracerta-api
  datasource:
    url: ${ORACLE_URL:jdbc:oracle:thin:@oracle.fiap.com.br:1521/ORCL}
    username: ${ORACLE_USER:rm563925}
    password: ${ORACLE_PASSWORD:070602}
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: create   # recria o schema a cada boot (facilita testes — trocar p/ update/validate depois)
    database-platform: org.hibernate.dialect.OracleDialect
    show-sql: false
  # Spring AI — DESABILITADO temporariamente (ver pom.xml). Reativar no PRD 04 com o upgrade de stack.
  # ai:
  #   google:
  #     genai:
  #       api-key: ${GEMINI_API_KEY:}
  #       chat:
  #         options:
  #           model: gemini-2.0-flash
  #           temperature: 0.2

safracerta:
  open-meteo:
    base-url: https://api.open-meteo.com/v1/forecast
    throttle-horas: 1   # só consulta nova previsão se a última for mais antiga que isto (horas)

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

server:
  port: 8080
